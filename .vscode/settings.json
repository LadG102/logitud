{
"liveServer.settings.useWebExt:": true,
"liveServer.settings.proxy": {
    "enable": true, //set it true to enable the feature.
    "baseUri": "/", //from where you want to proxy. 
    "proxyUri": "http://localhost:8000" //the actual url.
}
}